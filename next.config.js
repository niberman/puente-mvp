
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export',
  basePath: '/puente-mvp',
  images: {
    unoptimized: true,
  },
}
module.exports = nextConfig
